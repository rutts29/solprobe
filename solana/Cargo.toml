[workspace]
members = [
    "programs/solprobe-attestation",
    "programs/solprobe-escrow",
    "programs/solprobe-reputation",
    "programs/solprobe-staking",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
opt-level = 3
