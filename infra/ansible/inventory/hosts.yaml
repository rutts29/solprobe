all:
  children:
    gpu_nodes:
      hosts:
        gpu-node-0:
          ansible_host: 10.0.0.10
          node_id: node-0
        gpu-node-1:
          ansible_host: 10.0.0.11
          node_id: node-1
      vars:
        ansible_user: ubuntu
        ansible_python_interpreter: /usr/bin/python3
        backend_addr: "http://{{ backend_ip }}:50051"
        sidecar_metrics_port: 9100
