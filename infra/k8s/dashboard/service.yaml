apiVersion: v1
kind: Service
metadata:
  name: solprobe-dashboard
  namespace: solprobe
  labels:
    app: solprobe-dashboard
spec:
  type: ClusterIP
  selector:
    app: solprobe-dashboard
  ports:
    - name: http
      port: 3000
      targetPort: http
