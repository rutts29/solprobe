apiVersion: v1
kind: Service
metadata:
  name: solprobe-sidecar
  namespace: solprobe
  labels:
    app: solprobe-sidecar
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: solprobe-sidecar
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
