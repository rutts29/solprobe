global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "sidecar"
    static_configs:
      - targets: ["sidecar:9100"]

  - job_name: "backend"
    static_configs:
      - targets: ["backend:8000"]
